# Frontend env example. In dev the Vite proxy forwards /api to http://127.0.0.1:8000,
# so leave this empty. In production (Vercel), set this to the deployed backend URL,
# e.g. VITE_API_BASE_URL=https://your-render-app.onrender.com
VITE_API_BASE_URL=
